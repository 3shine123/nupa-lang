#include <stdio.h>
#import <Foundation/Foundation.nh>

int main() {
    @autoreleasepool {
        __block int counter = 0;
        void (^inc)(void) = ^void() {
            counter++;
        };
        inc();
        inc();
        inc();
        printf("counter = %d\n", counter);
    }
    return 0;
}
