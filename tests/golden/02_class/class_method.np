#include <stdio.h>
#import <Foundation/Foundation.nh>

@interface Factory : NPObject
+ (int)makeId;
@end

@implementation Factory
+ (int)makeId {
    return 42;
}
@end

int main() {
    int id = [Factory makeId];
    printf("ID: %d\n", id);
    return 0;
}
