#include <stdio.h>
#import <Foundation/Foundation.nh>

@interface Calculator : NPObject
- (int)add:(int)a to:(int)b;
@end

@implementation Calculator
- (int)add:(int)a to:(int)b {
    return a + b;
}
@end

int main() {
    Calculator *c = [[Calculator alloc] init];
    int result = [c add:3 to:4];
    printf("Result: %d\n", result);
    return 0;
}
