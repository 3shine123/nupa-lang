#include "nupa/runtime.h"
#import "Foundation/NPObject.nh"
#import "Foundation/NPObject.np"

@interface Item : NPObject {
    int _tag;
}
- (int)tag;
@end

@implementation Item

- (int)tag {
    return _tag;
}

@end

int main() {
    @noarc {
        Item *shared = [Item new];
        Item *alias = shared;
        [alias retain];
        [shared release];
        [alias release];
    }
    return 0;
}
