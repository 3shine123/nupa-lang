// test: NPLog with multiple format specifiers
#include <stdio.h>
#import <Foundation/Foundation.nh>

int main() {
    @autoreleasepool {
        NPLog(@"mix: %d + %s = ok", 42, "nupa");
        NPLog(@"just text");
    }
    printf("NPLog format OK\n");
    return 0;
}