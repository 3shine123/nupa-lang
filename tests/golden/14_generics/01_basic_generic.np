#include <stdio.h>
#import <Foundation/Foundation.nh>

@interface Box<T> : NPObject {
    @public
    T _value;
}
- (void)setValue:(T)val;
- (T)value;
@end

@implementation Box
- (void)setValue:(T)val {
    _value = val;
}
- (T)value {
    return _value;
}
@end

@interface Wrapper : NPObject
@end
@implementation Wrapper
@end

int main() {
    @autoreleasepool {
        Box<Wrapper*> *box = [[Box alloc] init];
        printf("Box<Wrapper*> created\n");
    }
    return 0;
}