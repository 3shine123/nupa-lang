#include <stdio.h>
#import <Foundation/Foundation.nh>

@interface Student : NPObject
@property int grade;
@end

int main() {
    Student *s = [[Student alloc] init];
    s.grade = 5;
    printf("Grade: %d\n", s.grade);
    return 0;
}
