#include <stdio.h>
#import <Foundation/Foundation.nh>

@interface Factory : NPObject
+ (instancetype)makeOne;
@end

@implementation Factory
+ (instancetype)makeOne {
    return [[self alloc] init];
}
@end

int main() {
    Factory *f = [Factory makeOne];
    printf("created\n");
    return 0;
}
