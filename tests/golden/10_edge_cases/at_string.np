#include <stdio.h>
#include <stdlib.h>
#import <Foundation/Foundation.nh>

int main() {
    @autoreleasepool {
        NPString *s = @"Hello from Micrit!";
        printf("%s\n", [s UTF8String]);
    }
    return 0;
}