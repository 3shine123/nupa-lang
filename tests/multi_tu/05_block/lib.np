#import "model.nh"
#include <stdio.h>

@implementation Pump : NPObject
- (void)onTick:(Ticker)handler {
    self->_handler = _Block_copy((const void *)handler);
}
- (void)fire:(int)n {
    if (self->_handler) self->_handler(n);
}
@end

int PumpHelper(void) { return 11; }
