#import "model.nh"

/* lib TU: implements `show` plus an extra method the header never declares, so
   this TU's uniform vtable has one more slot than the client TU's. */
@implementation Widget : NPObject
- (void)show { }
- (int)extraMethod { return 1; }
@end
